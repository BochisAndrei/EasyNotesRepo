<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.packg.easynotes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.CrossNoteActivity"></activity>
        <activity android:name=".Activitys.TextNoteActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activitys.SplashActivity"
            android:theme="@style/SplashTheme">

        </activity>
        <activity android:name=".Activitys.WellcomeLayout" />
        <activity android:name=".Activitys.Settings_layout" />
        <activity
            android:name=".Activitys.Sign_Up_layout"
            android:theme="@style/AppThemeTransparent" />
        <activity
            android:name=".Activitys.Login_layout"
            android:theme="@style/AppThemeTransparent" />
        <activity
            android:name=".Activitys.MainActivity"
            android:theme="@style/AppTheme.Transparent" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>